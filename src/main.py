# Importa la función que lanza la interfaz gráfica desde el archivo ui.py
from ui import iniciar_app

# Si este archivo se ejecuta directamente (no importado como módulo), lanza la app
if __name__ == "__main__":
    iniciar_app()  # Inicia la interfaz gráfica